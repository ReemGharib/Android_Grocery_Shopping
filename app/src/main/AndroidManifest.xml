<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.firstapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activities.Checkout"></activity>
        <activity android:name=".Activities.VerificationActivity" />
        <activity android:name=".Activities.Success" />
        <activity android:name=".Activities.NavigationItemActivity" />
        <activity android:name=".Activities.CartShopActivity" />
        <activity android:name=".Activities.CategoriesDetailsActivity" />
        <activity android:name=".Activities.ProductDetailsActivity" />
        <activity android:name=".Activities.FriutAndVegetableActivity" />
        <activity android:name=".Activities.SignUpActivity" />
        <activity android:name=".Activities.SignInAndForgotPassActivity" />
        <activity android:name=".Activities.MainActivity" />
        <activity android:name=".Activities.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>